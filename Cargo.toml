[package]
name = "pika"
version = "0.1.0"
edition = "2021"

[dependencies]
config = "0.13"
serde = { version = "1.0", features = ["derive"] }
aws-sdk-s3 = "0.28.0"
aws-config = "1.5.8"
tokio = { version = "1", features = ["full"] }